# Langfuse Bug Fix Loop Ledger

## Purpose
Track recent Langfuse sessions/traces inspected so this loop does not repeat the same evidence without new signals.

## Recently inspected
| Date | Session ID | Trace ID | Outcome | Notes |
| --- | --- | --- | --- | --- |
| 2026-03-09 | `conv_1773024143793_v7qtjonjy` | `session_1773024143795_wrbpe7lkn` | Not a product bug | `discord-recap-tweeter-v3` skill file was created after the failed run, so no code fix justified. |
| 2026-03-09 | `conv_1773028533776_ldsjpjgh5` | `session_1773028533777_0ya0av86q` | Fixed | Main agent over-delegated a simple repo question to `augustus`; user had to correct the system. |
| 2026-03-11 | `conv_1773175305930_77qxfmg2z` | `session_1773175401371_tauywx6ze` | Fixed | ACP run looped on `github:create_issue` because it kept sending `milestone: 0`; follow-up QA narrowed the fix to the traced GitHub placeholders only. |
| 2026-03-11 | `conv_1773174042460_wc5617nme` | `session_1773174042155_nf43e2z4v` | Fixed | Simple `Check our open GitHub issues` run retried on `github:list_issues` because it sent `since: ""`, which the GitHub MCP server rejected as `Invalid datetime for since`. |
| 2026-03-11 | `conv_1773175305930_77qxfmg2z` | `session_1773175401371_tauywx6ze` | Fixed (QA follow-up) | Re-inspected the same trace directly via Langfuse observations, added same-tool preservation coverage for valid `github:create_issue` options, and backfilled evidence provenance through commit `23569b546f010f9753852c62146bfb04d24ffb8b`. |
| 2026-03-11 | `conv_1773189947592_ng14sqy3e` | `session_1773189947593_ffkmg2v2t` | Fixed | ACP memory-hygiene run completed work, called `respond_to_user`, then still persisted a contradictory plain assistant failure message that made the run look unsuccessful. |
| 2026-03-11 | `conv_1773183195230_b4s0kbp7m` | `session_1773183195231_wljews235` | Fixed | A traced discrawl setup run emitted raw textual tool-call scaffolding (`[Calling tools: ...] RTLUjson ...`) instead of executing the tool, so the user had to send `continue` to unstick the flow. |
| 2026-03-11 | `conv_1773167393910_a4b6rdlma` | `session_1773167393911_m299f5ef8` | Fixed | Streaming textual tool directives were recovered too late, so raw `[Calling tools: execute_command] ...` scaffolding leaked into the conversation before the structured tool call executed. |
| 2026-03-11 | `conv_1773171104051_sl7f7hfsd` | `session_1773171104053_50hdwno0p` | Fixed | ACP surfaced raw `[list_repeat_tasks] ...` tool scaffolding as the final answer after `mark_work_complete`, so the user did not get a synthesized response in that run. |
| 2026-03-11 | `conv_1773202803243_hxenk4w14` | `session_1773202803254_f4vuinopr` | Fixed | A simple `Reply with exactly: session test ok` run died on a provider/API error, leaving `trace.output = null` and no persisted assistant failure message in conversation history. |
| 2026-03-11 | `conv_1773175182677_o15igzqp9` | `session_1773175182128_y5ykg6gnf` | Fixed | Desktop snoozed session tiles could render two maximize-style icons at once, and the traced run stalled for 10 iterations instead of landing the obvious UI fix. |
| 2026-03-11 | `conv_1773175182677_o15igzqp9` | `session_1773175182128_y5ykg6gnf` | Fixed (QA follow-up) | Replaced the snoozed-tile regression’s raw source-string assertion with a behavior-level rendered control test and backfilled the earlier evidence range through commit `39dc4c80f1fba1643ad09c51c05555f414d96d5a`. |
| 2026-03-11 | `conv_1773096124365_fjj1yime8` | `session_1773096124369_1a2itily5` | Fixed | Desktop agent-mode run surfaced raw bracketed builtin-tool scaffolding (`[respond_to_user]` / `[mark_work_complete]`) instead of recovering/executing the directives, so the user-facing response was not cleanly achieved in one run. |
| 2026-03-11 | `conv_1773169858587_atgwnegvl` | `session_1773169858587_am3y10svf` | Fixed | Semantic-search request for the user's notes stopped after `list_mcp_servers` reported enabled-but-disconnected `auggie mcp`, even though the app already had an in-process server restart path. |
| 2026-03-11 | `conv_1773170172508_3b6qdbyrw` | `session_1773170172508_3tfgr9fvs` | Fixed | Follow-up semantic-search request repeated the same blocked flow, confirming the agent lacked an in-session MCP reconnect path and prompt guidance for configured-but-disconnected servers. |
| 2026-03-11 | `conv_1773094660942_929o6zsae` | `session_1773094660944_1y8fnjuw6` | Fixed | A notes search hit `stdout maxBuffer length exceeded`; `execute_command` discarded partial stdout as a hard failure even though the buffered output already contained relevant matches, and the run later died without answering. |
| 2026-03-11 | `conv_1773169858587_atgwnegvl` | `session_1773169858587_am3y10svf` | Fixed (QA provenance follow-up) | Re-inspected the disconnected-`auggie mcp` diet-search traces and backfilled immutable evidence coverage through ledger handoff commit `1ad187942f0f7f71e18482c0fa86edc979222bb9`. |
| 2026-03-11 | `conv_1773094660942_929o6zsae` | `session_1773094660944_1y8fnjuw6` | Fixed (QA provenance follow-up) | Re-inspected the `execute_command` maxBuffer trace and backfilled immutable evidence coverage through ledger handoff commit `68e6c82b24c2118221104dfa9c91a0454ce5cd9d`. |
| 2026-03-11 | `conv_1773108880104_ftyd10pgn` | `session_1773108880106_dasx019ic` | Fixed (QA provenance follow-up) | Re-inspected the delegated `augustus` stale-stop traces, corrected the earlier evidence range so it truly covers docs handoff commit `e0d86917e25541e5aa566e59647c1a1b1ab6a20e`, and backfilled immutable provenance through ledger handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`. |
| 2026-03-11 | `conv_1773073608688_d7n7bj25t` | `session_1773073608689_lnqsyjou8` | Fixed | Main-agent worktree-setup run surfaced raw inline builtin-tool text (`[respond_to_user icode='...']`) instead of recovering and executing the `respond_to_user` tool call. |
| 2026-03-11 | `conv_1773077194696_a5e9ax0da` | `session_1773077194122_1fq71nkbc` | Fixed | A GitHub issue lookup for the mobile agent dropdown bug burned an avoidable retry on `github:search_issues` because the query omitted the required `is:issue`/`is:pull-request` filter, then the run ended without a final answer. |
| 2026-03-11 | `conv_1772999860748_wn4zhhi4k` | `session_1772999860411_w8cklaaln` | Fixed | A no-history `What should I do next?` prompt still launched tools, returned `trace.output = null`, and forced the user to send a follow-up because the agent lacked a runtime low-context guard. |

## Iteration notes

### 2026-03-09 - Prompt over-delegation fix
- Started by reviewing `apps/desktop/OBSERVABILITY.md` and `apps/desktop/DEBUGGING.md`.
- Dismissed one false lead where a skill genuinely did not exist yet.
- Chosen issue: mandatory delegation guidance caused a poor first run on a simple repo question.

#### Evidence
- Scope: Reduce unnecessary delegation in ACP main-agent runs when the task is simple enough to answer directly with available tools.
- Before evidence: Langfuse trace `session_1773028533777_0ya0av86q` shows the user asked `how does the QA agent work in aloops. will it work for any loop`; the first model step immediately called `delegate_to_agent` for `augustus`. In the same conversation (`conv_1773028533776_ldsjpjgh5`), the user later had to send `Use available tools directly via native function-calling, or provide a complete final answer.` The injected prompt in `apps/desktop/src/main/system-prompts.ts` explicitly said `ALWAYS delegate to a matching agent BEFORE responding` and `Only respond directly if NO agent matches the request`.
- Change: Updated `apps/desktop/src/main/system-prompts.ts` so delegation guidance prefers direct execution for simple/local tasks, reserves delegation for explicit/specialized/parallel cases, and tells the agent to incorporate delegated results into a complete answer. Added `apps/desktop/src/main/system-prompts.test.ts` coverage for the new wording.
- After evidence: The delegation prompt no longer contains the mandatory `ALWAYS delegate` / `Only respond directly if NO agent matches` rules, and now contains direct-answer preference plus delegation-result synthesis guidance. Regression test passes against the updated prompt builder.
- Verification commands/run results: `NODE_PATH="$HOME/Development/dotagents-mono/node_modules" "$HOME/Development/dotagents-mono/node_modules/.bin/vitest" run apps/desktop/src/main/system-prompts.test.ts` → passed (`2 tests`). Initial `pnpm --filter @dotagents/desktop test -- src/main/system-prompts.test.ts` was blocked because this worktree has no local `node_modules`.
- Blockers/remaining uncertainty: I did not perform a live desktop repro because this iteration only changed prompt text and the worktree lacks a local dependency install. Remaining lead: inspect whether delegated-run output selection should prefer explicit `respond_to_user` content more aggressively when sub-agents do run.

## Next promising leads
- Sessions where delegated sub-agent output was low quality or not synthesized cleanly by the parent agent
- Recent traces with tool/generation errors that map to app behavior instead of external provider/network failures
- Runs that ended without satisfying the user in a single agent execution

## Iteration notes

### 2026-03-11 - GitHub create_issue placeholder sanitization
- Reviewed the ledger first to avoid repeating prior traces, then inspected recent Langfuse traces with tool warnings and follow-up recovery.
- Chosen issue: trace `session_1773175401371_tauywx6ze` repeatedly called `github:create_issue` with `milestone: 0`, causing `Validation Failed` loops before the agent fell back to `gh issue create`.
- Reconstructed the failure in `apps/desktop/src/main/mcp-service.ts`, where MCP tool arguments were type-coerced but not sanitized for placeholder optional values.

#### Evidence
- Evidence ID: `github-create-issue-milestone-placeholder`
- Scope: Prevent ACP/MCP GitHub issue creation runs from retry-looping on invalid placeholder optional arguments that the GitHub MCP server rejects.
- Commit range: `e46d01822ce065cf4b6b0ee3aa64f0078c0cd00e..1e8428d1b2c817ab4235e40428197f03e866f3e0`
- Rationale: Langfuse showed a concrete agent-quality failure where the run burned multiple iterations on the same invalid `github:create_issue` payload instead of completing cleanly. Stripping placeholder optional values at the MCP execution layer prevents this specific validation loop and improves first-run success for user requests that create GitHub issues.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773175401371_tauywx6ze` in session `conv_1773175305930_77qxfmg2z` shows three `github:create_issue` calls failing with `Validation Failed` while sending optional placeholders including `milestone: 0`, `assignees: []`, and `labels: []`. The same run only completed after falling back to `gh issue create`, so the agent looped unnecessarily before achieving the user intent.
- Change: Initial fix updated `apps/desktop/src/main/mcp-service.ts` to sanitize the traced `github:create_issue` payload shape before execution and added regression coverage in `apps/desktop/src/main/mcp-service.option-b.test.ts` to assert the GitHub MCP client receives sanitized arguments. A later QA follow-up narrowed the implementation/evidence wording to the specific traced placeholders only; see the next entry.
- After evidence: Regression test exercised the traced payload shape and verified that `github:create_issue` executed without `milestone: 0` or empty optional arrays, matching the successful fallback payload shape more closely and eliminating the validation-triggering placeholder values before the MCP call was sent. The follow-up entry below records the additional guard against broader generic pruning.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/mcp-service.option-b.test.ts` initially failed because this worktree had no local `node_modules`. After linking this worktree to the existing sibling checkout dependencies, the same command passed (`6 tests`). A follow-up `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.node.json --noEmit` surfaced an unrelated pre-existing environment/type-resolution blocker: `src/main/acp/internal-agent.ts(16,30): error TS2307: Cannot find module 'uuid' or its corresponding type declarations.`
- Blockers/remaining uncertainty: No live desktop repro was needed because this was MCP tool wiring rather than UI behavior. The targeted regression test passed, but full desktop typecheck remains blocked in this worktree by the unrelated `uuid` resolution issue noted above.

### 2026-03-11 - GitHub create_issue scope correction after QA
- Reviewed the ledger and outstanding QA feedback first, then re-inspected session `conv_1773175305930_77qxfmg2z` / trace `session_1773175401371_tauywx6ze` via `langfuse-cli`.
- Confirmed from the traced generation payload that the failing MCP calls specifically carried `assignees: []`, `labels: []`, and `milestone: 0`; the evidence did not justify broad pruning of empty strings, nulls, or unrelated-tool optional fields.
- Chosen follow-up: narrow the sanitization to the traced `github:create_issue` placeholders and add a regression test proving unrelated tools still receive empty optional values unchanged.

#### Evidence
- Evidence ID: `github-create-issue-milestone-placeholder`
- Scope: Align the GitHub issue-creation placeholder fix with the actual Langfuse evidence by stripping only the traced invalid `github:create_issue` placeholders and avoiding broader optional-argument pruning.
- Commit range: `1e8428d1b2c817ab4235e40428197f03e866f3e0..3a7d0725abbc295b376a4a668c79db14834416a3`
- Rationale: The prior implementation solved the traced loop but overreached by pruning empty/null optional values for every MCP tool without corresponding evidence. Narrowing the behavior to the traced GitHub placeholders preserves the first-run fix while reducing regression risk for other tools that may legitimately distinguish between omitted and explicitly empty optional values.
- QA feedback: Addressed reviewer finding that the previous change/evidence overclaimed generic optional-argument pruning when the Langfuse trace only showed `github:create_issue` placeholders (`milestone: 0`, `assignees: []`, `labels: []`).
- Before evidence: Langfuse trace `session_1773175401371_tauywx6ze` (session `conv_1773175305930_77qxfmg2z`) still shows the failing generation emitting `github:create_issue` tool arguments with `assignees: []`, `labels: []`, and `milestone: 0` before the MCP server returned `Validation Failed`. Re-inspection via `langfuse-cli` did not surface evidence for empty-string/null pruning on unrelated tools, so the broader sanitization from the prior commit was not trace-backed.
- Change: Updated `apps/desktop/src/main/mcp-service.ts` to strip only the traced GitHub `create_issue` placeholders (`assignees: []`, `labels: []`, `milestone <= 0`). Extended `apps/desktop/src/main/mcp-service.option-b.test.ts` with a guard test that unrelated tools still receive empty-string, empty-array, and null optional values unchanged.
- After evidence: The GitHub regression test still verifies that traced placeholder arguments are removed before `github:create_issue` executes, while the new unrelated-tool regression test verifies that empty optional values are no longer pruned generically. This brings the implementation and the evidence back into scope alignment.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/mcp-service.option-b.test.ts` → passed (`7 tests`, exit 0). Vitest emitted a pre-existing `apps/mobile/tsconfig.json` parse warning about resolving `expo/tsconfig.base`, but the targeted desktop test file still completed successfully.
- Blockers/remaining uncertainty: No live desktop repro was needed because this iteration only changed MCP argument sanitization for a traced tool call. I revalidated against the same Langfuse trace, but I did not broaden verification beyond the targeted desktop regression file because the change intentionally narrowed scope.

### 2026-03-11 - GitHub create_issue QA regression hardening
- Reviewed the ledger first and picked up the outstanding QA feedback instead of choosing a new trace.
- Re-inspected session `conv_1773175305930_77qxfmg2z` / trace `session_1773175401371_tauywx6ze` directly via the Langfuse public API and confirmed generation observations `ddc3cec8-c635-4d54-be54-b7363f92f3f1`, `3e02cfa6-5bf0-438f-8a2e-b80604c06bc3`, and `79dac91d-b9da-450e-89b8-2b74776a760d` each queued `github:create_issue` with `assignees: []`, `milestone: 0`, and `labels: []` before the repeated `Validation Failed` loop.
- Chosen follow-up: backfill immutable evidence provenance across the post-`3a7d0725...` review-stack commits and add a same-tool regression proving legitimate `github:create_issue` option values survive unchanged.

#### Evidence
- Evidence ID: `github-create-issue-milestone-placeholder`
- Scope: Close the remaining QA gaps on the traced GitHub issue-creation loop by recording immutable provenance through the current handoff commit and proving same-tool valid optional arguments are preserved.
- Commit range: `3a7d0725abbc295b376a4a668c79db14834416a3..23569b546f010f9753852c62146bfb04d24ffb8b`
- Rationale: Previous QA correctly noted that commits `4afaf4100a3e735531f0d83e5023e08552aa035b`, `e4f34223d4b92abafeb06c992e310ff48a32426e`, and `d29d5f50d23a5ce522eccd1acf4bfd80eb12b043` changed the ledger history without any Evidence block covering them, and that the narrowed sanitization claim lacked a same-tool regression. Backfilling provenance and adding the preserving-values regression closes both confidence gaps without broadening runtime behavior beyond what the trace supports.
- QA feedback: Addressed reviewer findings that (1) commits after `3a7d0725abbc295b376a4a668c79db14834416a3` lacked Evidence coverage and (2) the narrowed `github:create_issue` claim needed a same-tool regression proving positive `milestone` / non-empty `assignees` / `labels` survive unchanged.
- Before evidence: Direct re-inspection of Langfuse trace `session_1773175401371_tauywx6ze` in session `conv_1773175305930_77qxfmg2z` showed generation observations `ddc3cec8-c635-4d54-be54-b7363f92f3f1`, `3e02cfa6-5bf0-438f-8a2e-b80604c06bc3`, and `79dac91d-b9da-450e-89b8-2b74776a760d` each queued `github:create_issue` with `assignees: []`, `milestone: 0`, and `labels: []` before repeated `Validation Failed` tool errors. The earlier regression suite only proved those placeholders were stripped and that unrelated tools kept empty optional values; it did not yet prove that valid `github:create_issue` optional values on the same tool were preserved.
- Change: Added a targeted regression in `apps/desktop/src/main/mcp-service.option-b.test.ts` asserting that `github:create_issue` still forwards legitimate optional values (`assignees: ["aj47"]`, `milestone: 12`, `labels: ["bug", "desktop"]`) unchanged. This handoff also backfills immutable evidence provenance for the post-`3a7d0725...` review-stack commits listed above plus handoff commit `23569b546f010f9753852c62146bfb04d24ffb8b`.
- After evidence: `apps/desktop/src/main/mcp-service.option-b.test.ts` now covers both sides of the trace-backed contract: placeholder `milestone: 0` / empty arrays are removed for `github:create_issue`, while a same-tool case verifies positive `milestone` and non-empty `assignees` / `labels` remain intact. That closes the remaining mismatch between the Langfuse evidence and the test coverage.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/mcp-service.option-b.test.ts` → passed (`8 tests`, exit 0). Vitest still emitted a pre-existing `apps/mobile/tsconfig.json` parse warning about resolving `expo/tsconfig.base`, but the targeted desktop regression file completed successfully.
- Blockers/remaining uncertainty: No live desktop repro was needed because this iteration only strengthened regression coverage around previously narrowed MCP argument sanitization. Full repo-wide type/config validation remains noisy in this worktree because of the pre-existing mobile `expo/tsconfig.base` resolution warning under the current local Node/tooling setup.

### 2026-03-11 - ACP trailing failure text after respond_to_user
- Reviewed `langfuse-bug-fix.md` first, confirmed no outstanding QA feedback file existed at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected recent Langfuse traces with warning/error signals.
- Chosen issue: session `conv_1773189947592_ng14sqy3e` / trace `session_1773189947593_ffkmg2v2t` failed the memory-hygiene loop despite completing the requested work and recording a valid `respond_to_user` payload.
- Langfuse observations showed the run loaded the `memory-hygiene` skill, listed memories, recovered from one failed `execute_command`, successfully appended the report, successfully called `respond_to_user`, then still ended with plain assistant text `I couldn't complete the request after multiple attempts...`.
- Reconstructed the same contradiction in the stored conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773189947592_ng14sqy3e.json`, which contained the successful `respond_to_user` tool call followed by the bogus trailing assistant failure message.

#### Evidence
- Evidence ID: `acp-respond-to-user-trailing-failure`
- Scope: Prevent ACP runs that already produced a valid current-prompt `respond_to_user` result from persisting contradictory trailing plain assistant fallback/failure text as the final conversation outcome.
- Commit range: `23569b546f010f9753852c62146bfb04d24ffb8b..cca0a6a8970eac49147a513e830e758daaa1bda8`
- Rationale: The traced memory-hygiene run achieved the user's intent in a single agent execution, but the app still persisted a later fallback failure string after `respond_to_user`. That contradiction makes a successful run appear failed in conversation history and can mislead the UI or downstream tracing/output selection.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773189947593_ffkmg2v2t` in session `conv_1773189947592_ng14sqy3e` shows `load_skill_instructions`, `list_memories`, a warning `execute_command`, a successful retry `execute_command`, and a successful `respond_to_user` call with completion text. The same run still ended with trace output `I couldn't complete the request after multiple attempts...`. The persisted conversation file for that session likewise contained the successful `respond_to_user` tool call followed by the contradictory trailing assistant failure text.
- Change: Updated `apps/desktop/src/main/acp-main-agent.ts` so completion handling inspects only current-prompt `respond_to_user` calls, prunes trailing plain assistant text that appears after a successful current-prompt `respond_to_user`, and treats the run as complete when that user-facing response exists. Added a regression in `apps/desktop/src/main/acp-main-agent.test.ts` covering the exact traced sequence, including persistence. Later ledger-only handoff commit `cca0a6a8970eac49147a513e830e758daaa1bda8` recorded the immutable evidence range for that fix.
- After evidence: The new ACP regression simulates a completed `respond_to_user` followed by trailing assistant fallback text and verifies the returned/final response remains the user-facing `respond_to_user` text, the completion is marked successful, and the contradictory assistant failure text is not present in persisted conversation history.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/acp-main-agent.test.ts` → passed (`10 tests`, exit 0). Vitest emitted pre-existing environment noise in this worktree: an unsupported Node engine warning for local Node `v25.2.1` and the existing `apps/mobile/tsconfig.json` parse warning for missing `expo/tsconfig.base`, but the targeted desktop ACP test file still completed successfully.
- Blockers/remaining uncertainty: I did not run a live Electron repro because the Langfuse trace plus persisted conversation file were enough to isolate the non-UI output-selection bug and the targeted ACP regression now covers the traced sequence. Remaining leads should focus on other traces where ACP emitted fallback/error text after a successful user-facing tool response, especially multi-tool skill runs that recover from an intermediate tool warning.

### 2026-03-11 - Recover textual tool directives from Langfuse-traced stalled run
- Reviewed `langfuse-bug-fix.md` first, then read outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing new work.
- Addressed the prior QA provenance gaps in this ledger by correcting the broken ACP evidence SHA/range and widening the earlier GitHub scope-correction range so commits `cdb34fd523739a2ffa5ddd6da9e5d011fd8da5cc` and `cca0a6a8970eac49147a513e830e758daaa1bda8` are now covered.
- Chosen issue: Langfuse session `conv_1773183195230_b4s0kbp7m` / trace `session_1773183195231_wljews235` showed a user asking Discrawl setup questions; during the run the assistant emitted raw text `[Calling tools: iterm:read_terminal_output] RTLUjson {...}` into the conversation instead of executing the tool, and the user had to send `continue` to resume the task.
- Reconstructed the failure from both Langfuse observations and the persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773183195230_b4s0kbp7m.json`, which showed the raw textual tool directive persisted as an assistant message before the `continue` recovery.

#### Evidence
- Evidence ID: `textual-tool-directive-recovery`
- Scope: Recover single text-encoded tool directives in the desktop LLM fetch layer so ACP runs continue automatically instead of leaking raw `[Calling tools: ...]` scaffolding to the user and stalling until a follow-up prompt.
- Commit range: `cca0a6a8970eac49147a513e830e758daaa1bda8..8630750c33c79a17e2725406c4092bead240dbbe`
- Rationale: The traced Discrawl setup run did not achieve the user intent cleanly in one pass because the model emitted a textual tool directive rather than a native tool-call event, and the app treated that text as a user-visible assistant message. Recovering the unambiguous single-tool pattern at the fetch layer keeps the run moving in the same agent execution and avoids another user-facing stall/`continue` recovery.
- QA feedback: Addressed reviewer findings that the ledger used a broken `bf4caed...` end SHA and left commits `cdb34fd523739a2ffa5ddd6da9e5d011fd8da5cc` / `cca0a6a8970eac49147a513e830e758daaa1bda8` uncovered; this iteration also fixes a new Langfuse-traced stalled-run bug.
- Before evidence: Langfuse trace `session_1773183195231_wljews235` in session `conv_1773183195230_b4s0kbp7m` includes a generation/output segment that surfaced raw assistant text beginning `[Calling tools: iterm:read_terminal_output]` followed by `RTLUjson` and a JSON payload instead of a native tool-call observation. The persisted conversation file for the same session likewise contains that raw assistant message and then a user follow-up `continue`, showing the run stalled and required manual recovery.
- Change: Updated `apps/desktop/src/main/llm-fetch.ts` to recover an unambiguous single textual tool directive of the traced shape (`[Calling tools: ...]` plus JSON arguments) into a structured tool call for both `generateText` and streaming `streamText` paths. Added targeted regressions in `apps/desktop/src/main/llm-fetch.test.ts` covering both the non-streaming and streaming forms of the traced output.
- After evidence: The new `llm-fetch` regressions now simulate the exact traced textual directive pattern and verify the desktop fetch layer returns a structured `toolCalls` payload with no user-facing `content`, which allows ACP to execute the tool instead of persisting raw scaffolding text. That directly closes the traced stall mode where the user had to send `continue`.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/llm-fetch.test.ts` → passed (`29 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.json --noEmit` → passed (exit 0). Both commands emitted pre-existing environment noise only: an unsupported Node engine warning for local Node `v25.2.1`, and Vitest also printed the existing `apps/mobile/tsconfig.json` parse warning for unresolved `expo/tsconfig.base`, but the targeted desktop test and typecheck both completed successfully.
- Blockers/remaining uncertainty: I did not run a live Electron repro because the runtime profile for this iteration is `none`, and the Langfuse trace plus persisted conversation file were enough to reconstruct the exact stalled flow. The recovery intentionally handles only unambiguous single-tool textual directives; traces with multi-tool textual scaffolding would need separate evidence before broadening the parser.

### 2026-03-11 - Buffer streaming textual tool directives before they leak
- Reviewed `langfuse-bug-fix.md` first, confirmed there was still no outstanding QA feedback file at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected fresh Langfuse traces/observations instead of reusing already-fixed sessions.
- Chosen issue: session `conv_1773167393910_a4b6rdlma` / trace `session_1773167393911_m299f5ef8` still leaked raw streaming scaffold text even though the tool call was eventually represented structurally.
- Langfuse observation `f01f81a4-76a0-4103-8ef5-1902c0585128` captured `Streaming LLM Call` output beginning `[Calling tools: execute_command] to=execute_command...`, while later observation `6c8557cb-396b-4a97-847f-d77a68816bae` for the same trace showed the recovered structured `toolCalls` payload. The persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773167393910_a4b6rdlma.json` likewise contained an assistant message with the raw scaffold text plus the structured `execute_command` tool call.
- Reconstructed the cause in `apps/desktop/src/main/llm-fetch.ts`: `makeLLMCallWithStreamingAndTools()` streamed every text delta immediately via `onChunk()` and only attempted `extractTextualToolDirective()` after the stream ended, so recovered single-tool textual directives could still leak into progress/conversation state before recovery completed.

#### Evidence
- Evidence ID: `streaming-textual-tool-buffering`
- Scope: Prevent recovered single-tool textual directives from leaking raw `[Calling tools: ...]` scaffold text during streaming before the desktop fetch layer converts them into structured tool calls.
- Commit range: `8630750c33c79a17e2725406c4092bead240dbbe..751431f01ad937b7413638c58c69c3c4ceb26ee2`
- Rationale: The traced semantic-search assistance run did not achieve the user intent cleanly in one pass because the app surfaced raw tool scaffolding mid-stream before the recovered `execute_command` call took over. Buffering that narrow bracket-prefixed directive shape until recovery is known preserves normal streaming for real text while removing a user-visible leak that made the run look broken/stalled.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773167393911_m299f5ef8` in session `conv_1773167393910_a4b6rdlma` includes `Streaming LLM Call` observation `f01f81a4-76a0-4103-8ef5-1902c0585128` whose output starts with raw assistant text `[Calling tools: execute_command] to=execute_command...`. The same trace later contains `Streaming LLM Call` observation `6c8557cb-396b-4a97-847f-d77a68816bae` with the corresponding structured `toolCalls` payload, and the persisted conversation file for that session shows the raw scaffold text attached to the assistant message alongside the `execute_command` tool call. That combination indicates recovery happened too late to stop the streamed scaffold from leaking.
- Change: Updated `apps/desktop/src/main/llm-fetch.ts` so the streaming+tools path buffers text while it still matches the single-tool textual-directive prefix and only flushes it if no directive is recovered. Added targeted regressions in `apps/desktop/src/main/llm-fetch.test.ts` proving (1) a trace-shaped streaming directive now yields a recovered `toolCalls` result without any streamed text chunks, and (2) bracket-prefixed plain text still flushes through when it is not actually a tool directive.
- After evidence: The new `llm-fetch` streaming regression now simulates the traced `to=...` scaffold shape and verifies `onChunk` is never called before the recovered `iterm:read_terminal_output` tool call is returned. A companion regression confirms buffered bracket-prefixed plain text is flushed normally, so the fix stays tightly scoped to the Langfuse-traced leak instead of broadly disabling streaming for bracketed content.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/llm-fetch.test.ts` → passed (`30 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc --noEmit -p tsconfig.node.json` → failed, but only because of pre-existing unrelated repo/environment issues: `src/main/acp-main-agent.test.ts(528,9)` tuple typing and `src/main/acp/internal-agent.ts(16,30)` missing `uuid` types. Both commands also emitted the existing Node engine warning (`v25.2.1`) and Vitest again printed the known `apps/mobile/tsconfig.json` `expo/tsconfig.base` parse warning.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not start Electron. The fix intentionally targets only the narrow single-tool textual-directive streaming leak evidenced by this trace. Remaining leads: investigate raw streaming outputs of the form `[respond_to_user] { ... }` / `[mark_work_complete] { ... }`, which still appear in other Langfuse observations and may need a separate, trace-backed treatment.

### 2026-03-11 - ACP mark_work_complete fallback for raw tool scaffolding
- Reviewed `langfuse-bug-fix.md` first, then read outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing new work.
- Deferred the older QA concerns on `streaming-textual-tool-buffering` for this pass and instead chose a fresh Langfuse-backed ACP completion bug with a clearer user-facing failure. I still fixed the scoped `acp-main-agent.test.ts` tuple typing issue that QA had called out while touching the same test file.
- Chosen issue: session `conv_1773171104051_sl7f7hfsd` / trace `session_1773171104053_50hdwno0p` shows the user asking whether any repeat tasks should be turned on; the run ended with raw output beginning `[list_repeat_tasks] {"success":true,...}` instead of a synthesized answer.
- Reconstructed the same failure from the persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773171104051_sl7f7hfsd.json`, which showed raw assistant tool-scaffolding text, a `mark_work_complete` tool call, its tool result, and then the same raw scaffolding text again with no `respond_to_user` call.
- Mapped the failure to `apps/desktop/src/main/acp-main-agent.ts`, where ACP completion preferred any trailing plain `result.response` / accumulated text even when it was just bracketed tool scaffolding, and had no fallback path for a valid `mark_work_complete` summary.

#### Evidence
- Evidence ID: `acp-mark-work-complete-fallback`
- Scope: Prevent ACP runs from surfacing raw bracketed tool scaffolding as the final answer when the agent completes with `mark_work_complete` but fails to send `respond_to_user`.
- Commit range: `751431f01ad937b7413638c58c69c3c4ceb26ee2..2ef66cf098c7e493ef7028844f72a8c5eea45557`
- Rationale: The traced repeat-task run did real work and even signaled completion, but the app still delivered raw `[list_repeat_tasks] ...` scaffold text to the user. Falling back to the completion summary, while stripping persisted scaffold text from assistant messages that host tool calls, restores a usable one-run answer and avoids making a successful ACP completion look broken.
- QA feedback: Deferred prior QA findings on the older `streaming-textual-tool-buffering` iteration for this pass; this iteration instead fixes a new ACP completion fallback bug, while also resolving the scoped `apps/desktop/src/main/acp-main-agent.test.ts` tuple typecheck issue noted by QA.
- Before evidence: Langfuse trace `session_1773171104053_50hdwno0p` in session `conv_1773171104051_sl7f7hfsd` shows the final output as raw scaffold text starting `[list_repeat_tasks] {"success":true,...}`. The persisted conversation file for the same session contains that raw assistant text, a `mark_work_complete` tool call with summary `Reported the enabled repeat tasks to the user.`, the tool result, and then the same raw scaffold text again—clear evidence that the user never received the synthesized answer in that single run.
- Change: Updated `apps/desktop/src/main/acp-main-agent.ts` so ACP completion now (1) strips bracketed tool-scaffolding text from assistant messages that host tool calls, (2) derives the latest `mark_work_complete` summary from the current prompt, (3) prunes trailing plain assistant leftovers after a completion tool signal when no `respond_to_user` exists, and (4) falls back to the completion summary when the only candidate final text is raw tool scaffolding. Extended `apps/desktop/src/main/acp-main-agent.test.ts` with a regression that mirrors the traced sequence and fixed the mock-call typing that was breaking `typecheck:node` in this file.
- After evidence: The new ACP regression simulates the traced raw `[list_repeat_tasks] ...` output plus `mark_work_complete` sequence and now verifies the returned/final response becomes `Reported the enabled repeat tasks to the user.`, the persisted conversation history no longer contains the scaffold text, and the run still completes successfully without a `respond_to_user` call.
- Verification commands/run results: `pnpm install --ignore-scripts --frozen-lockfile` → passed after the initial targeted test attempt exposed missing local `node_modules` / `tsup`. `pnpm --filter @dotagents/desktop test -- --run src/main/acp-main-agent.test.ts` → passed (`11 tests`, exit 0). `pnpm --filter @dotagents/desktop run typecheck:node` → passed (exit 0). All commands emitted the existing unsupported Node engine warning for local Node `v25.2.1`, but the targeted desktop regression and node typecheck both completed successfully after the worktree install.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not start Electron for a live repro. This fallback intentionally prefers the `mark_work_complete` summary only when ACP does not produce a clean non-scaffold final text; traces where ACP emits other raw builtin-tool text forms such as `[respond_to_user] { ... }` or `[mark_work_complete] { ... }` remain good future leads.

### 2026-03-11 - Persist terminal assistant errors for blank failed runs
- Reviewed `langfuse-bug-fix.md` first, confirmed there was no outstanding QA feedback file at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected fresh recent Langfuse traces instead of revisiting previously fixed sessions.
- Chosen issue: session `conv_1773202803243_hxenk4w14` / trace `session_1773202803254_f4vuinopr` shows a simple prompt `Reply with exactly: session test ok` failing on the first streamed LLM call and ending with `trace.output = null`.
- Langfuse observations for that trace showed `Streaming LLM Call` at `ERROR` with status `Failed after 3 attempts. Last error: Cannot connect to API:`. The persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773202803243_hxenk4w14.json` contained only the user message, so the failed run had no saved assistant-side failure response at all.
- Reconstructed the gap in `apps/desktop/src/main/llm.ts`, where unexpected terminal errors were rethrown before any final assistant failure text was appended/persisted, while the `finally` block still closed Langfuse with empty `finalContent`.

#### Evidence
- Evidence ID: `terminal-agent-error-persistence`
- Scope: Ensure unexpected terminal agent failures in desktop agent-mode runs persist a user-visible assistant error message to conversation history and Langfuse output instead of leaving the run blank/null.
- Commit range: `2ef66cf098c7e493ef7028844f72a8c5eea45557..66f635f4dd04ac8102e878c00f3c2c3b60927fa2`
- Rationale: The traced run failed in a user-visible way, but reopening the conversation or inspecting Langfuse made it look like the assistant never answered at all. Persisting a terminal assistant error message preserves what happened, prevents silent-looking failed runs, and makes retries/debugging much more intelligible when upstream provider/API outages occur.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773202803254_f4vuinopr` in session `conv_1773202803243_hxenk4w14` recorded input `Reply with exactly: session test ok`, `trace.output = null`, and a `Streaming LLM Call` observation at `ERROR` with status `Failed after 3 attempts. Last error: Cannot connect to API:`. The persisted conversation file for the same session contained only the user message, confirming the failure was not recorded as an assistant response in saved history.
- Change: Added `apps/desktop/src/main/agent-terminal-error.ts` to centralize user-visible terminal-error message formatting plus one-time assistant-message append/persist behavior. Updated `apps/desktop/src/main/llm.ts` so unexpected terminal failures now synthesize that assistant error message before rethrowing, ensuring `endAgentTrace()` sees non-empty final output and conversation history is updated. Added targeted regression coverage in `apps/desktop/src/main/agent-terminal-error.test.ts`.
- After evidence: The new regression verifies a traced provider-style error (`Cannot connect to API:`) is converted into a clean assistant failure message and appended/persisted only once. `apps/desktop` node typecheck also passes with the new helper wired into `llm.ts`, so future trace-backed failures through this path should record a visible assistant error instead of a blank/null terminal run.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/agent-terminal-error.test.ts` → passed (`2 tests`, exit 0). `pnpm --filter @dotagents/desktop run typecheck:node` → passed (exit 0). Both commands emitted the existing unsupported Node engine warning for local Node `v25.2.1`, but the targeted regression and desktop node typecheck completed successfully.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not start Electron for a live repro. This fix intentionally covers the `processTranscriptWithAgentMode()` terminal-error path evidenced by the trace; other run modes with separate terminal-failure handling may still warrant independent Langfuse-backed review if they also produce blank/null failed runs.

### 2026-03-11 - Desktop snoozed tile duplicate maximize control
- Reviewed `langfuse-bug-fix.md` first, confirmed there was still no outstanding QA feedback file at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected fresh recent Langfuse traces instead of revisiting already-fixed sessions.
- Chosen issue: session `conv_1773175182677_o15igzqp9` / trace `session_1773175182128_y5ykg6gnf` shows the user reporting that the sessions tile still had two maximize icons in some cases.
- Langfuse observations show the run spent 10 iterations reading `apps/desktop/src/renderer/src/pages/sessions.tsx` and `apps/desktop/src/renderer/src/components/agent-progress.tsx`, then still fell back to `I couldn't complete the request after multiple attempts...` without making the obvious fix.
- Reconstructing the traced code path showed the bug directly: the tile header rendered a generic `Maximize tile` control whenever `onExpand && !isExpanded`, while the same snoozed tile also rendered a `Restore session` control with the same `Maximize2` icon, producing duplicate maximize-style affordances.

#### Evidence
- Evidence ID: `desktop-snoozed-tile-duplicate-maximize`
- Scope: Prevent desktop session tiles from showing duplicate maximize-style controls when a snoozed session already has a dedicated restore action.
- Commit range: `66f635f4dd04ac8102e878c00f3c2c3b60927fa2..bdfb487850226baeb1eebbbeb3d5370469645b5f`
- Rationale: The traced run failed a straightforward user-visible cleanup even after reaching the right file and code region, so the sessions UI kept presenting two near-identical maximize affordances for snoozed tiles. Hiding the generic maximize control in the snoozed state removes the duplicate visual affordance and makes the restore action unambiguous.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773175182128_y5ykg6gnf` in session `conv_1773175182677_o15igzqp9` records the user report about duplicate maximize icons, repeated repo inspection commands, and a final fallback failure after `totalIterations: 10`. In the traced desktop code path, `apps/desktop/src/renderer/src/components/agent-progress.tsx` rendered `title="Maximize tile"` under `onExpand && !isExpanded` and also rendered `title="Restore session"` with the same `Maximize2` icon under `isSnoozed`, which is sufficient source-level evidence for the duplicated control state. Runtime profile is `none`, so no PR-visible before screenshot was captured.
- Change: Updated `apps/desktop/src/renderer/src/components/agent-progress.tsx` so the tile-level maximize button is suppressed when `progress.isSnoozed` is true, leaving only the dedicated restore control for snoozed tiles. Added a targeted regression in `apps/desktop/src/renderer/src/components/agent-progress.tile-layout.test.ts` asserting the maximize control now requires `!isSnoozed` while the restore action remains present. Mobile has no equivalent maximize/restore tile chrome, so no cross-app change was needed.
- After evidence: The desktop renderer source now gates the generic maximize button behind `onExpand && !isExpanded && !isSnoozed`, so a snoozed tile keeps only the restore affordance instead of two maximize-style icons. `src/renderer/src/components/agent-progress.tile-layout.test.ts` passes with the new regression, giving a stable check for the exact traced duplicate-control condition. Runtime profile is `none`, so no PR-visible after screenshot was captured.
- Verification commands/run results: `pnpm vitest run src/renderer/src/components/agent-progress.tile-layout.test.ts` (cwd `apps/desktop`) → passed (`13 tests`, exit 0). An initial mis-invocation `pnpm --dir apps/desktop vitest run ...` failed immediately with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "apps/desktop" not found`; the rerun from the package directory passed cleanly.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron or capture before/after screenshots; the fallback evidence for this UI-facing iteration is the Langfuse trace plus direct source-path reconstruction and targeted regression coverage. Remaining lead: inspect whether any other snoozed-session surfaces outside `AgentProgress` reuse a generic maximize affordance alongside restore, though the mobile session list has no equivalent desktop tile chrome.

### 2026-03-11 - Desktop snoozed tile QA regression hardening
- Reviewed `langfuse-bug-fix.md` first and picked up the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` instead of choosing a new trace.
- Re-inspected session `conv_1773175182677_o15igzqp9` / trace `session_1773175182128_y5ykg6gnf` via `langfuse-cli` and confirmed the same user-facing failure: the run exhausted 10 iterations, never shipped the straightforward tile fix, and ended with `I couldn't complete the request after multiple attempts...`.
- Chosen follow-up: strengthen the snoozed-tile regression from a raw source substring check to a rendered tile-control assertion, and backfill immutable provenance for the earlier ledger-only handoff commit.

#### Evidence
- Evidence ID: `desktop-snoozed-tile-duplicate-maximize`
- Scope: Close the remaining QA gaps on the traced snoozed-session tile issue by proving rendered control behavior and backfilling immutable evidence provenance through the latest handoff commit.
- Commit range: `bdfb487850226baeb1eebbbeb3d5370469645b5f..39dc4c80f1fba1643ad09c51c05555f414d96d5a`
- Rationale: The original UI fix was correct, but QA correctly noted that the regression only checked source text and that the handoff ledger commit itself was left uncovered. Rendering `AgentProgress` in a narrow mocked runtime and asserting the actual button titles gives the strongest non-runtime proof available under runtime profile `none`, while the updated provenance closes the review-stack bookkeeping gap.
- QA feedback: Addressed reviewer findings that (1) commit `bdfb487850226baeb1eebbbeb3d5370469645b5f` was uncovered by any Evidence block and (2) `apps/desktop/src/renderer/src/components/agent-progress.tile-layout.test.ts` only asserted raw source substrings instead of rendered snoozed-tile behavior.
- Before evidence: Langfuse trace `session_1773175182128_y5ykg6gnf` in session `conv_1773175182677_o15igzqp9` already showed the user reporting duplicate maximize icons, the agent rereading the relevant tile files for 10 iterations, and the run still failing without a fix. In the repo before this QA follow-up, the only regression for that condition in `apps/desktop/src/renderer/src/components/agent-progress.tile-layout.test.ts` was a raw substring assertion against `'{onExpand && !isExpanded && !isSnoozed && ('`, so the test did not yet prove what controls a snoozed tile actually renders.
- Change: Reworked the targeted snoozed-tile regression in `apps/desktop/src/renderer/src/components/agent-progress.tile-layout.test.ts` to import and render `AgentProgress` with a mocked React runtime, mocked renderer dependencies, and representative tile progress data. The test now asserts that an active tile exposes `Maximize tile` but not `Restore session`, while a snoozed tile exposes `Restore session` but not `Maximize tile`. I also backfilled the earlier Evidence block’s commit range so the prior handoff commit `bdfb487850226baeb1eebbbeb3d5370469645b5f` is no longer left outside provenance.
- After evidence: The updated regression now exercises rendered header controls instead of source text and passes with the exact traced states: non-snoozed tiles still expose the generic maximize action, while snoozed tiles expose only the dedicated restore action. The earlier Evidence block for this trace now also covers its former ledger-only handoff commit, and this QA follow-up range covers the current handoff commit `39dc4c80f1fba1643ad09c51c05555f414d96d5a`.
- Verification commands/run results: `pnpm vitest run src/renderer/src/components/agent-progress.tile-layout.test.ts` (cwd `apps/desktop`) → passed (`13 tests`, exit 0).
- Blockers/remaining uncertainty: Runtime profile is still `none`, so I did not launch Electron or capture before/after screenshots. The new rendered test is the strongest available fallback evidence for this UI-facing QA pass, but broader live UI validation of snoozed session tiles remains deferred until a desktop runtime iteration explicitly requires it.

### 2026-03-11 - Recover bracketed builtin textual tool directives
- Reviewed `langfuse-bug-fix.md` first and read the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing work.
- Explicitly deferred that older unresolved QA feedback for this pass because it concerns the earlier `load_skill_instructions` default-skill narrowing, while this iteration fixes a fresher Langfuse-backed user-facing run failure.
- Chosen issue: session `conv_1773096124365_fjj1yime8` / trace `session_1773096124369_1a2itily5` shows the user asking `can you use the skill to see the current loops progress`; the traced run finished with raw output beginning `[respond_to_user] { ... }` and the persisted conversation file duplicated raw bracketed builtin-tool text instead of clean structured tool execution + final delivery.
- Reconstructed the failure from Langfuse trace/output plus the persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773096124365_fjj1yime8.json`, which showed the current prompt ending with repeated assistant messages beginning `[respond_to_user] { ... }`.
- Mapped the gap to `apps/desktop/src/main/llm-fetch.ts`, where textual tool recovery only recognized the `[Calling tools: ...]` form and only recovered a single directive, so direct bracketed builtin-tool output like `[respond_to_user] { ... }\n[mark_work_complete] { ... }` fell through as plain text.

#### Evidence
- Evidence ID: `bracketed-builtin-tool-directives`
- Scope: Recover direct bracketed builtin-tool textual directives in the desktop LLM fetch layer so agent-mode runs execute `respond_to_user` / `mark_work_complete` instead of leaking raw scaffolding text into history/output.
- Commit range: `39dc4c80f1fba1643ad09c51c05555f414d96d5a..151cfba37cd8f55f76b7ec60facf4638dfe8527c`
- Rationale: The traced loop-progress run did gather the right information, but the user did not get a clean one-run outcome because the model's bracketed builtin-tool text was treated as assistant content instead of executable tool directives. Recovering that narrow, trace-backed format removes a real user-facing failure mode without broadening behavior beyond known tool names.
- QA feedback: Deferred older unresolved QA findings from the 2026-03-08 `load_skill_instructions` default-skill iteration for this pass; this iteration instead fixes a new Langfuse-backed raw builtin-tool scaffolding failure.
- Before evidence: Langfuse trace `session_1773096124369_1a2itily5` in session `conv_1773096124365_fjj1yime8` records input `can you use the skill to see the current loops progress` and final `trace.output` beginning `[respond_to_user] { ... }` rather than a clean delivered answer. The persisted conversation file for that same session also ended with repeated assistant messages starting `[respond_to_user] { ... }`, confirming that direct bracketed builtin-tool text leaked through the desktop pipeline as plain content instead of structured tool calls/results.
- Change: Updated `apps/desktop/src/main/llm-fetch.ts` so textual-tool recovery now recognizes direct bracketed known-tool names (for example `[respond_to_user]` / `[mark_work_complete]`) in addition to the older `[Calling tools: ...]` form, and it can recover multiple sequential directives in one response. Added targeted regressions in `apps/desktop/src/main/llm-fetch.test.ts` covering both non-streaming and streaming recovery of the traced builtin-tool pattern. This final handoff commit is ledger-only and records the immutable evidence range through substantive fix commit `5a6a9a79d402b2bde88003f8a1c9439f34c8b8ce`.
- After evidence: The new `llm-fetch` regressions now simulate the traced bracketed builtin-tool output and verify the desktop fetch layer returns structured `toolCalls` for both `respond_to_user` and `mark_work_complete` with no user-facing `content`, including the streaming path where raw bracketed scaffolding previously could have leaked. That keeps the agent on the tool-execution path instead of persisting raw scaffolding as the answer.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/llm-fetch.test.ts` → passed (`32 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.node.json --noEmit` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. The parser intentionally stays narrow by only treating direct bracketed names as tool directives when they resolve to known provided tools; if Langfuse later shows other raw-tool text formats, they should be traced and added separately rather than inferred.

### 2026-03-11 - Recover configured-but-disconnected MCP servers in-session
- Reviewed `langfuse-bug-fix.md` first and read the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing work.
- Repaired the ledger plan first: identified that several earlier Evidence blocks still left docs-only handoff commits uncovered (`8630750c...`, `751431f0...`, `2ef66cf0...`, `66f635f4...`, `151cfba3...`) and updated those ranges before recording new evidence.
- Chosen issue: Langfuse traces `session_1773169858587_am3y10svf` (`conv_1773169858587_atgwnegvl`) and `session_1773170172508_3tfgr9fvs` (`conv_1773170172508_3b6qdbyrw`) show two near-identical semantic-search requests about the user's diet both stopping after `load_skill_instructions("augment-context-search")` and `list_mcp_servers`, with the agent telling the user that `auggie mcp` was enabled but disconnected and that a restart was required.
- Reconstructed the repo-side gap in `apps/desktop/src/main/builtin-tools.ts` and `apps/desktop/src/main/system-prompts.ts`: the desktop app already exposed `mcpService.restartServer(...)`, but the model-facing `toggle_mcp_server` tool only changed config and told the agent to restart later, and the repo-owned system prompt provided no recovery guidance for configured-but-disconnected servers.

#### Evidence
- Evidence ID: `mcp-disconnected-server-recovery`
- Scope: Let desktop agent-mode runs recover a configured-but-disconnected MCP server in-session instead of immediately failing user requests that depend on that server.
- Commit range: `151cfba37cd8f55f76b7ec60facf4638dfe8527c..f12b426a324c874ee230fc620f10ce0004bdeeb6`
- Rationale: The traced semantic-search requests did not achieve the user's intent in a single run because the agent had no practical in-session recovery path once `list_mcp_servers` reported `auggie mcp` as enabled but disconnected. Making the existing server-toggle tool apply immediately and teaching the in-repo prompt to try that path once reduces repeat blocked runs without inventing a new tool surface.
- QA feedback: Addressed reviewer findings that earlier ledger entries still had stale/uncovered docs-only commit provenance; this iteration also fixes a new Langfuse-backed semantic-search/MCP recovery failure.
- Before evidence: Langfuse trace `session_1773169858587_am3y10svf` in session `conv_1773169858587_atgwnegvl` shows the agent loading the semantic-search skill, calling `list_mcp_servers`, then responding `I can’t run semantic search right now because your \`auggie mcp\` server is enabled but **not connected** in this session. Please restart the app/agent mode and rerun this request...`. Follow-up trace `session_1773170172508_3tfgr9fvs` in session `conv_1773170172508_3b6qdbyrw` repeats the same flow on the same underlying intent (`use semantic search to find what i should be eating daily`), confirming the agent lacked an in-session reconnect path instead of merely suffering a one-off refusal.
- Change: Updated `apps/desktop/src/main/builtin-tools.ts` so `toggle_mcp_server` trims the server name, persists the runtime-enabled state, immediately calls `mcpService.restartServer(...)` when enabling a disconnected/enabled server, immediately calls `mcpService.stopServer(...)` when disabling, and returns the actual runtime outcome (`connected`, `runtimeAction`, `runtimeError`). Updated `apps/desktop/src/main/builtin-tool-definitions.ts` to describe the new immediate behavior, added repo-owned MCP recovery guidance to `apps/desktop/src/main/system-prompts.ts`, added focused execution tests in `apps/desktop/src/main/builtin-tools.toggle-mcp-server.test.ts`, and extended `apps/desktop/src/main/system-prompts.test.ts` with a regression for the new guidance. This final handoff commit is ledger-only and records the immutable evidence range through substantive fix commit `f12b426a324c874ee230fc620f10ce0004bdeeb6`.
- After evidence: The new builtin-tool regression verifies `toggle_mcp_server({ serverName: "auggie mcp", enabled: true })` now triggers `restartServer(...)` immediately and reports the server as connected, while disabling triggers `stopServer(...)` immediately. The system-prompt regression verifies the desktop prompt now explicitly tells the agent to try `toggle_mcp_server` once and re-check `list_mcp_servers` before telling the user a restart is required, closing the exact dead-end reflected in both Langfuse traces.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/builtin-tools.toggle-mcp-server.test.ts src/main/system-prompts.test.ts` → passed (`5 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.node.json --noEmit` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live desktop repro. The semantic-search skill text itself is external to this repo and still mentions restart-on-disconnect, but the in-repo system prompt now adds later recovery guidance and the existing toggle tool now has the immediate reconnect behavior that those traces were missing.

### 2026-03-11 - Preserve partial execute_command stdout on maxBuffer overflow
- Reviewed `langfuse-bug-fix.md` first and read the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing work.
- Deferred the older unresolved QA provenance follow-up for `mcp-disconnected-server-recovery` this pass because a fresher Langfuse trace showed a concrete user-facing tool failure with a smaller, fully testable repo fix.
- Chosen issue: session `conv_1773094660942_929o6zsae` / trace `session_1773094660944_1y8fnjuw6` shows the user correcting the request to `no notes not nose`; the agent then tried a broad `grep` across `~/Documents/agent-notes`, hit a maxBuffer failure, and the run later ended with `(Agent mode was stopped by emergency kill switch)` instead of answering the notes question.
- Reconstructed the gap in `apps/desktop/src/main/builtin-tools.ts`, where `execute_command` treated `ERR_CHILD_PROCESS_STDIO_MAXBUFFER` like any other hard error even when Node had already buffered useful partial stdout.

#### Evidence
- Evidence ID: `execute-command-maxbuffer-partial-output`
- Scope: Preserve usable partial stdout from `execute_command` when a shell command exceeds the Node `exec` stdout buffer, instead of converting the entire tool call into a hard failure.
- Commit range: `1ad187942f0f7f71e18482c0fa86edc979222bb9..027757f5ce684c30dba5aff18669edf55a789e1e`
- Rationale: The traced notes-search run already had the relevant result in buffered stdout, but the desktop tool surfaced only a hard failure because the command also spilled into a huge archived export. Returning structured partial success keeps the agent moving with the evidence it already has and reduces the chance of oversized-search loops or stalls on user-facing searches.
- QA feedback: Deferred prior outstanding QA feedback about `mcp-disconnected-server-recovery` immutable provenance for this iteration; chose a fresher Langfuse-backed `execute_command` overflow failure instead.
- Before evidence: Langfuse trace `session_1773094660944_1y8fnjuw6` in session `conv_1773094660942_929o6zsae` records input `no notes not nose` and later ends with `(Agent mode was stopped by emergency kill switch)`. Tool observation `d103c96b-f5a0-42a4-bcb6-881a06a15053` shows `execute_command` running `ls -la ~/Documents/agent-notes && grep -Rin "chloe" ~/Documents/agent-notes || true` and returning `success: false`, `error: "stdout maxBuffer length exceeded"`, and `exitCode: "ERR_CHILD_PROCESS_STDIO_MAXBUFFER"` even though the captured stdout already included a relevant hit in `personal/aj-notion-knowledge.md` before the oversized `archive/claude-export/conversations.json` dump consumed the buffer. A later retry observation `6d74af4e-5bd0-4a21-b78d-474d0915cb8e` narrowed the grep but still returned a failure payload, so the run never converted the partial evidence into a useful answer.
- Change: Updated `apps/desktop/src/main/builtin-tools.ts` so `execute_command` now shares one stdout-truncation helper across success/error paths and treats `ERR_CHILD_PROCESS_STDIO_MAXBUFFER` with non-empty stdout as a structured partial success (`success: true`, `partialOutput: true`, `bufferExceeded: true`, truncation hint) instead of a hard tool failure. Added `apps/desktop/src/main/builtin-tools.execute-command.test.ts` to cover both the traced maxBuffer-overflow path and the unchanged ordinary-error path.
- After evidence: The new regression simulates the traced maxBuffer condition and now verifies `execute_command` returns a non-error partial-success payload with truncated stdout plus guidance for narrowing the command, while a companion test proves normal command failures still surface as errors. That means future broad note searches like the traced run can still hand the agent the buffered matches it already found instead of discarding them outright.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/builtin-tools.execute-command.test.ts` → passed (`2 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.node.json --noEmit` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. This fix intentionally only upgrades the traced maxBuffer-with-stdout case; commands that overflow without any buffered stdout still return an error, and very broad searches should still be narrowed when possible to avoid noisy partial results.

### 2026-03-11 - QA provenance backfill for MCP reconnect + maxBuffer evidence
- Reviewed `langfuse-bug-fix.md` first and picked up the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` instead of choosing a new trace.
- Re-inspected Langfuse trace `session_1773169858587_am3y10svf` plus follow-up trace `session_1773170172508_3tfgr9fvs` and confirmed both still show the same disconnected-`auggie mcp` dead end that the earlier `toggle_mcp_server` / prompt fix addressed.
- Re-inspected Langfuse trace `session_1773094660944_1y8fnjuw6` plus `execute_command` observations `d103c96b-f5a0-42a4-bcb6-881a06a15053` and `6d74af4e-5bd0-4a21-b78d-474d0915cb8e`, confirming the maxBuffer run still matches the previously documented partial-stdout failure mode.
- Chosen follow-up: repair the ledger so both evidence IDs truthfully cover the docs-only handoff commits that QA identified as still uncovered.

#### Evidence
- Evidence ID: `mcp-disconnected-server-recovery`
- Scope: Backfill immutable ledger provenance for the traced configured-but-disconnected MCP recovery fix through its docs-only handoff commit.
- Commit range: `f12b426a324c874ee230fc620f10ce0004bdeeb6..1ad187942f0f7f71e18482c0fa86edc979222bb9`
- Rationale: QA correctly noted that the earlier semantic-search/MCP evidence stopped at the substantive fix commit even though the later handoff commit `1ad187942f0f7f71e18482c0fa86edc979222bb9` changed the ledger and therefore also needed immutable provenance. Re-inspecting the same Langfuse traces confirmed the product diagnosis remains unchanged, so the correct fix for this pass is to repair the evidence bookkeeping rather than alter runtime behavior again.
- QA feedback: Addressed reviewer finding that Evidence ID `mcp-disconnected-server-recovery` still left ledger-only handoff commit `1ad187942f0f7f71e18482c0fa86edc979222bb9` uncovered.
- Before evidence: Langfuse trace `session_1773169858587_am3y10svf` in session `conv_1773169858587_atgwnegvl` still shows the agent stopping after `list_mcp_servers` and telling the user a restart was required because `auggie mcp` was enabled but disconnected. Follow-up trace `session_1773170172508_3tfgr9fvs` in session `conv_1773170172508_3b6qdbyrw` repeats the same blocked flow on the same underlying semantic-search intent. In the ledger, the prior Evidence block for this fix ended at substantive commit `f12b426a324c874ee230fc620f10ce0004bdeeb6`, leaving the later docs-only handoff commit `1ad187942f0f7f71e18482c0fa86edc979222bb9` outside any immutable Evidence range.
- Change: Updated `langfuse-bug-fix.md` to record this explicit QA follow-up and to backfill the earlier MCP recovery iteration's immutable provenance through handoff commit `1ad187942f0f7f71e18482c0fa86edc979222bb9`, with no code-path changes because the existing repo fix and tests remain valid.
- After evidence: The ledger now contains a dedicated Evidence block whose commit range ends at `1ad187942f0f7f71e18482c0fa86edc979222bb9`, so the traced MCP reconnect fix and its docs-only handoff commit are both covered by immutable provenance.
- Verification commands/run results: Re-inspected the affected Langfuse traces via `npx -y langfuse-cli api traces get session_1773169858587_am3y10svf --json` and `npx -y langfuse-cli api traces get session_1773170172508_3tfgr9fvs --json`, and confirmed git commit ancestry with `git log --oneline --decorate -n 20`.
- Blockers/remaining uncertainty: This pass only repairs ledger provenance; it does not re-run the earlier desktop tests because no runtime code changed. The semantic-search skill text outside this repo still mentions restart-on-disconnect, so future traces should still be monitored for mismatches between external skill guidance and the in-repo recovery path.

#### Evidence
- Evidence ID: `execute-command-maxbuffer-partial-output`
- Scope: Backfill immutable ledger provenance for the traced `execute_command` maxBuffer partial-stdout fix through its docs-only handoff commit.
- Commit range: `027757f5ce684c30dba5aff18669edf55a789e1e..68e6c82b24c2118221104dfa9c91a0454ce5cd9d`
- Rationale: QA correctly noted that the earlier maxBuffer Evidence block stopped at substantive fix commit `027757f5ce684c30dba5aff18669edf55a789e1e`, even though later handoff commit `68e6c82b24c2118221104dfa9c91a0454ce5cd9d` modified the ledger for the same traced issue. Re-inspecting the Langfuse trace and tool observations confirmed the underlying product fix remains the right one, so this pass should repair provenance rather than perturb the working runtime behavior.
- QA feedback: Addressed reviewer finding that Evidence ID `execute-command-maxbuffer-partial-output` did not cover review head `68e6c82b24c2118221104dfa9c91a0454ce5cd9d`, which was itself a ledger-only handoff commit.
- Before evidence: Langfuse trace `session_1773094660944_1y8fnjuw6` in session `conv_1773094660942_929o6zsae` still ends with `(Agent mode was stopped by emergency kill switch)`, and observation `d103c96b-f5a0-42a4-bcb6-881a06a15053` still shows `execute_command` returning `stdout maxBuffer length exceeded` despite already buffering relevant note hits. Observation `6d74af4e-5bd0-4a21-b78d-474d0915cb8e` confirms the later narrowed grep still returned a failure payload. In the ledger, the earlier Evidence block for this fix ended at substantive commit `027757f5ce684c30dba5aff18669edf55a789e1e`, leaving later docs-only handoff commit `68e6c82b24c2118221104dfa9c91a0454ce5cd9d` uncovered.
- Change: Updated `langfuse-bug-fix.md` to record this explicit QA follow-up and to backfill the earlier `execute_command` maxBuffer iteration's immutable provenance through handoff commit `68e6c82b24c2118221104dfa9c91a0454ce5cd9d`, with no code changes because the previously added builtin-tool fix and regression coverage already match the trace.
- After evidence: The ledger now contains a dedicated Evidence block whose commit range ends at `68e6c82b24c2118221104dfa9c91a0454ce5cd9d`, so the traced maxBuffer partial-output fix and its docs-only handoff commit are both covered by immutable provenance.
- Verification commands/run results: Re-inspected the affected Langfuse trace via `npx -y langfuse-cli api traces get session_1773094660944_1y8fnjuw6 --json` and the key tool observations via `npx -y langfuse-cli api observations get d103c96b-f5a0-42a4-bcb6-881a06a15053 --json` and `npx -y langfuse-cli api observations get 6d74af4e-5bd0-4a21-b78d-474d0915cb8e --json`; also confirmed the reviewed commit stack with `git log --oneline --decorate -n 20`.
- Blockers/remaining uncertainty: This pass only repairs ledger provenance; it does not re-run the earlier desktop tests because no runtime code changed. The current handoff commit for this pass will itself need the usual immutable-provenance follow-up in a later review-stack pass because Git commit content cannot self-reference its own final SHA.

### 2026-03-11 - Retry delegated ACP prompts after stale stop-state failures
- Reviewed `langfuse-bug-fix.md` first and read the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing work.
- Picked up the unresolved QA provenance finding as part of this iteration by correcting the delegated ACP evidence range to begin at parent commit `68e6c82b24c2118221104dfa9c91a0454ce5cd9d`, which truthfully covers prior docs-only handoff commit `e0d86917e25541e5aa566e59647c1a1b1ab6a20e`.
- Chosen issue: Langfuse trace `session_1773108880106_dasx019ic` in session `conv_1773108880104_ftyd10pgn` shows the user asking `how are the aloops . use the skill`; the run called `delegate_to_agent` for `augustus` and then returned `(Agent mode was stopped by emergency kill switch)` instead of answering. Supporting trace `session_1773107816822_s1abxovde` on the same underlying aloops-status intent ended with `output = null` after `list_available_agents` and `delegate_to_agent`, showing the delegated path could also fail silently.
- Reconstructed the failing flow from Langfuse trace details, the persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773108880104_ftyd10pgn.json`, and `apps/desktop/src/main/acp-service.ts`: synchronous delegated ACP runs reused existing ACP sessions, and `sendPrompt()` surfaced stop-related failures directly, so a stale delegated session stop could abort a new delegated task instead of forcing a fresh sub-agent session.

#### Evidence
- Evidence ID: `delegated-acp-stale-stop-retry`
- Scope: Recover synchronous delegated ACP runs when a reused sub-agent session reports a stale kill-switch/emergency-stop failure, instead of surfacing the stop or leaving the parent run blank.
- Commit range: `68e6c82b24c2118221104dfa9c91a0454ce5cd9d..fe05802cb8b3b67d4579bcfd7d26c6bc15cb0105`
- Rationale: The traced aloops-status request did not achieve the user’s intent in a single run because the parent agent successfully delegated to `augustus` and then got back an emergency-stop failure from the delegated path, even though the user had not asked to stop anything. Retrying once with a fresh ACP session is a minimal, trace-backed recovery that preserves normal stop semantics while preventing stale delegated session state from poisoning a new task.
- QA feedback: Addressed reviewer finding that prior docs-only review head `e0d86917e25541e5aa566e59647c1a1b1ab6a20e` was still uncovered by any Evidence block; a later docs-only handoff for this same traced fix is recorded in the follow-up block below.
- Before evidence: Langfuse trace `session_1773108880106_dasx019ic` in session `conv_1773108880104_ftyd10pgn` records the user input `how are the aloops . use the skill`, a `delegate_to_agent` tool call for `augustus`, and final output `(Agent mode was stopped by emergency kill switch)`. The persisted conversation file for that session shows the same sequence: user request → delegated tool call → emergency-stop assistant reply, with no successful aloops summary. Supporting trace `session_1773107816822_s1abxovde` shows the same delegated aloops-status path ending with `trace.output = null` after `list_available_agents` and `delegate_to_agent`, which matches the pre-fix `acpService.sendPrompt()` behavior of reusing existing ACP sessions and not recovering stop-related empty/error responses.
- Change: Updated `apps/desktop/src/main/acp-service.ts` so `sendPrompt()` now recognizes stop-related delegated ACP failures (`Session stopped by kill switch` / `Aborted by emergency stop`), retries once through `getOrCreateSession(agentName, true)` to force a fresh ACP session when the desktop app itself is not currently stopping, and otherwise returns a real error instead of an empty success when a stop-like response has no text. Added a focused regression in `apps/desktop/src/main/acp-service.test.ts` proving a stale delegated stop failure is retried once with a fresh session and then returns the recovered output.
- After evidence: The new ACP service regression now simulates the traced failure mode by making the first delegated `session/prompt` call fail with `Session stopped by kill switch` and verifying the desktop app immediately requests a fresh ACP session and reissues the prompt, returning the recovered delegated output instead of surfacing the stop. That keeps synchronous `delegate_to_agent(..., waitForResult: true)` runs on the happy path when the failure came from stale delegated session state rather than an intentional active stop.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/acp-service.test.ts` → passed (`31 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc -p tsconfig.node.json --noEmit` (cwd `apps/desktop`) → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live delegated-agent repro. This fix intentionally retries only stop-like failures once and only when the current desktop app is not actively stopping; if future Langfuse traces show other delegated-session reuse failures, they should be handled with trace-specific recovery.

### 2026-03-11 - QA provenance backfill for delegated ACP stale-stop retry
- Reviewed `langfuse-bug-fix.md` first and picked up the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` instead of choosing a new trace.
- Re-inspected Langfuse trace `session_1773108880106_dasx019ic` plus supporting trace `session_1773107816822_s1abxovde`, confirming both still reflect the same delegated stale-stop failure mode that the earlier `acp-service.ts` retry fix addressed.
- Chosen follow-up: repair the ledger so the delegated ACP evidence truthfully covers prior docs-only handoff commit `e0d86917e25541e5aa566e59647c1a1b1ab6a20e` and the already-reviewed docs-only handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`.

#### Evidence
- Evidence ID: `delegated-acp-stale-stop-retry`
- Scope: Backfill immutable ledger provenance for the traced delegated ACP stale-stop retry fix through docs-only handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`.
- Commit range: `fe05802cb8b3b67d4579bcfd7d26c6bc15cb0105..f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`
- Rationale: QA correctly noted that the prior delegated ACP Evidence block both overclaimed coverage of docs-only handoff commit `e0d86917e25541e5aa566e59647c1a1b1ab6a20e` and stopped before later docs-only handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`. Re-inspecting the same Langfuse traces confirmed the product diagnosis and code fix remain unchanged, so the correct scope for this pass is to repair the immutable provenance rather than alter working runtime behavior.
- QA feedback: Addressed reviewer findings that (1) the earlier `delegated-acp-stale-stop-retry` commit range excluded `e0d86917e25541e5aa566e59647c1a1b1ab6a20e` despite claiming to start there and (2) review head `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9` was not covered by any Evidence block.
- Before evidence: Langfuse trace `session_1773108880106_dasx019ic` in session `conv_1773108880104_ftyd10pgn` still shows the user’s aloops-status request delegating to `augustus` and ending with `(Agent mode was stopped by emergency kill switch)` instead of a useful summary. Supporting trace `session_1773107816822_s1abxovde` still ends with `trace.output = null` after the delegated path. In the ledger before this follow-up, the prior delegated ACP Evidence block used commit range `e0d86917e25541e5aa566e59647c1a1b1ab6a20e..fe05802cb8b3b67d4579bcfd7d26c6bc15cb0105`, which excluded `e0d...`, and no later Evidence block covered docs-only handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`.
- Change: Updated `langfuse-bug-fix.md` to correct the earlier delegated ACP Evidence range to `68e6c82b24c2118221104dfa9c91a0454ce5cd9d..fe05802cb8b3b67d4579bcfd7d26c6bc15cb0105` and added this dedicated QA follow-up block covering docs-only handoff commit `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`, with no code changes because the previously landed `acp-service.ts` fix and regression coverage already match the trace.
- After evidence: The ledger now has truthful immutable provenance for the delegated stale-stop fix across both docs-only handoff commits identified by QA: the corrected earlier range now includes `e0d86917e25541e5aa566e59647c1a1b1ab6a20e`, and this dedicated follow-up range ends at `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`.
- Verification commands/run results: Re-inspected the delegated failure observations via `npx -y langfuse-cli api observations list --trace-id session_1773108880106_dasx019ic --limit 5 --json` and confirmed reviewed commit ancestry with `git log --oneline --decorate -n 20` plus `git rev-parse e0d86917e25541e5aa566e59647c1a1b1ab6a20e^`.
- Blockers/remaining uncertainty: This pass only repairs ledger provenance; it does not re-run the earlier desktop tests because no runtime code changed. The current handoff commit for this QA pass will itself still need the usual immutable-provenance backfill in a later review-stack iteration because Git commit content cannot self-reference its own final SHA.

### 2026-03-11 - Strip github list_issues empty since placeholders
- Reviewed `langfuse-bug-fix.md` first, confirmed there was still no outstanding QA feedback file at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected fresh recent Langfuse traces instead of revisiting already-fixed sessions.
- Chosen issue: session `conv_1773174042460_wc5617nme` / trace `session_1773174042155_nf43e2z4v` handled the user request `Check our open GitHub issues.` but only after burning an extra retry on a bad placeholder argument.
- Direct Langfuse observation re-inspection showed `github:list_issues` warning observation `a83294d6-590f-4f69-b243-c6410d019d39` sent `since: ""` against `aj47/dotagents-mono` and failed with `Validation Error: Invalid datetime for since`; a later observation `a879a20a-da91-4ced-9992-c4c5f9fbdc69` succeeded only after the agent replaced that field with `1970-01-01T00:00:00Z`.
- Reconstructed the fix in `apps/desktop/src/main/mcp-service.ts`, where we already had trace-backed placeholder sanitization for `github:create_issue` but not for the traced `github:list_issues` `since` placeholder.

#### Evidence
- Evidence ID: `github-list-issues-empty-since`
- Scope: Prevent simple GitHub issue-listing runs from wasting retries on the traced `github:list_issues` placeholder `since: ""` value that the GitHub MCP server rejects.
- Commit range: `f122b1b80ac0f66768dab76288ff2e5e9b8a4da9..fb8147fcc4a620118b5e0ed327970a32d27fca18`
- Rationale: The traced run did eventually answer the user, but it performed poorly on a simple repo-status request because the desktop MCP layer forwarded an empty-string datetime placeholder that the GitHub server immediately rejected. Stripping only that traced invalid placeholder improves first-pass success without broadening sanitization beyond the evidence.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773174042155_nf43e2z4v` in session `conv_1773174042460_wc5617nme` shows a warning `github:list_issues` observation `a83294d6-590f-4f69-b243-c6410d019d39` with arguments `owner: "aj47"`, `repo: "dotagents-mono"`, `state: "open"`, and `since: ""`, which failed with `Validation Error: Invalid datetime for since`. The same trace only completed after a later `github:list_issues` observation `a879a20a-da91-4ced-9992-c4c5f9fbdc69` retried with synthetic fallback `since: "1970-01-01T00:00:00Z"` and returned `[]`, so the user intent was achieved only after an avoidable extra iteration.
- Change: Updated `apps/desktop/src/main/mcp-service.ts` to strip only the traced `github:list_issues` empty-string `since` placeholder before execution. Added focused regressions in `apps/desktop/src/main/mcp-service.option-b.test.ts` proving (1) the traced empty-string `since` value is removed while `labels: []` remains intact, and (2) a legitimate ISO `since` value on the same tool is preserved unchanged. This final handoff commit is ledger-only and records immutable evidence through substantive fix commit `fb8147fcc4a620118b5e0ed327970a32d27fca18`.
- After evidence: `apps/desktop/src/main/mcp-service.option-b.test.ts` now covers both sides of the traced contract for `github:list_issues`: the empty-string `since` placeholder is stripped before the MCP call, while a real timestamp still flows through untouched. That matches the Langfuse recovery path and removes the traced validation-error retry from future runs.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/mcp-service.option-b.test.ts` → passed (`10 tests`, exit 0). Vitest emitted only pre-existing environment noise in this worktree: an unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: I did not launch Electron because the runtime profile for this iteration is `none` and the bug is isolated to MCP argument sanitization rather than renderer behavior. The same trace also contains an earlier `github:list_issues` `Not Found` attempt on repo `dotagents` (observation `77b9e0f4-2ca7-4306-9515-00fcb207878e`), which suggests a separate repo-inference quality issue that I intentionally did not address in this narrow fix.

### 2026-03-11 - Recover inline quoted builtin-tool directives
- Reviewed `langfuse-bug-fix.md` first, confirmed the outstanding QA feedback file still did not exist at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and scanned recent Langfuse traces for unledgered failures instead of revisiting already-covered sessions.
- Chosen issue: session `conv_1773073608688_d7n7bj25t` / trace `session_1773073608689_lnqsyjou8` handled a worktree-setup request, but the first run finished with raw inline builtin-tool scaffolding instead of a delivered user response.
- Direct Langfuse observation re-inspection showed `Streaming LLM Call` observation `4a3a9707-b5bb-4863-aa47-8fdbce1e15ed` outputting raw text beginning `[respond_to_user icode='{"text":"Done — I created a fresh isolated worktree from \`main\`...` rather than a structured tool call, while the same trace still reached `Tool: mark_work_complete` span `cd46b264-93a4-4430-b937-eb39d4de2f1e`.
- Reconstructed the same leak in the persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773073608688_d7n7bj25t.json`, where assistant message index `38` stored the raw `[respond_to_user icode='...']` text as plain content.

#### Evidence
- Evidence ID: `inline-quoted-builtin-tool-directives`
- Scope: Recover the traced inline quoted builtin-tool directive form so desktop runs do not surface raw `[respond_to_user icode='...']` scaffolding when the model encodes tool arguments inside the bracket payload.
- Commit range: `fb8147fcc4a620118b5e0ed327970a32d27fca18..e178fbec8e94e6ed04b0dd3e1b22985837e8aa9e`
- Rationale: The traced run achieved the underlying work but failed to satisfy the user cleanly in a single agent execution because the desktop fetch layer only recovered plain `[tool] {json}` / `[Calling tools: ...]` scaffolding, not the inline-quoted builtin-tool variant the model actually emitted. Recovering that narrow traced format prevents raw tool text from leaking into conversation history and lets the builtin user-response tool execute as intended.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773073608689_lnqsyjou8` in session `conv_1773073608688_d7n7bj25t` ended with `trace.output` equal to raw inline tool text beginning `[respond_to_user icode='{"text":"Done — I created a fresh isolated worktree from \`main\`...` instead of a delivered answer. Observation `4a3a9707-b5bb-4863-aa47-8fdbce1e15ed` recorded the same raw `Streaming LLM Call` content, and the persisted conversation file for that session stored the raw scaffolding at assistant message index `38`, confirming the directive leaked all the way through the desktop pipeline as plain text.
- Change: Updated `apps/desktop/src/main/llm-fetch.ts` to recognize and recover inline quoted JSON tool directives for known tools, including the traced builtin form `[respond_to_user icode='{"text":"..."}']`, in both the non-streaming `generateText` path and the streaming buffer/recovery path. Added focused regressions in `apps/desktop/src/main/llm-fetch.test.ts` covering both `generateText` and streaming recovery for the traced inline-quoted builtin-tool shape.
- After evidence: The new `llm-fetch` regressions now simulate the traced inline builtin-tool syntax and verify the fetch layer returns a structured `respond_to_user` tool call with no user-facing content. The streaming regression also verifies `onChunk` is never called for the buffered `[respond_to_user icode='...']` sequence, so the traced raw-text leak no longer reaches conversation/progress output in this path.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/llm-fetch.test.ts` → passed (`34 tests`, exit 0). `pnpm --filter @dotagents/desktop run typecheck:node` → passed (exit 0). Both commands emitted only pre-existing environment noise in this worktree: an unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. This fix intentionally targets only the traced inline-quoted JSON directive format for known tools; if Langfuse later shows other malformed bracket syntaxes, they should be added separately based on concrete evidence rather than inferred.

### 2026-03-11 - Default github search_issues queries to issue filter when missing type
- Reviewed `langfuse-bug-fix.md` first, confirmed the outstanding QA feedback file still did not exist at `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt`, and inspected fresh Langfuse traces with `output = null` or avoidable MCP warnings instead of revisiting already-fixed sessions.
- Chosen issue: session `conv_1773077194696_a5e9ax0da` / trace `session_1773077194122_1fq71nkbc` handled a request about an existing mobile `.agents` issue, but the run burned an early GitHub search validation error and later ended without a final answer.
- Direct Langfuse observation re-inspection showed warning observation `25a2da3b-4a0c-450f-a64b-68751b44a745` calling `github:search_issues` with `q: "repo:aj47/dotagents-mono mobile session agent dropdown can't select"`, which failed with `Validation Error: Query must include 'is:issue' or 'is:pull-request'`. The same persisted conversation later showed three corrected `github:search_issues` calls with explicit `is:issue` filters, confirming the missing qualifier was an avoidable wasted iteration in the failing run.
- Reconstructed the fix in `apps/desktop/src/main/mcp-service.ts`, where traced GitHub placeholder sanitization already existed for other MCP calls, but `github:search_issues` still forwarded missing-type queries unchanged.

#### Evidence
- Evidence ID: `github-search-issues-missing-type-filter`
- Scope: Prevent traced GitHub issue-search runs from burning a validation-error retry when `github:search_issues` omits the required `is:issue` / `is:pull-request` qualifier.
- Commit range: `e178fbec8e94e6ed04b0dd3e1b22985837e8aa9e..b8b95732bd77f4ba0627ee2726d1b641b450fa6c`
- Rationale: The traced mobile-issue lookup failed to satisfy the user in one clean run: it first sent a GitHub MCP search query that the server could never accept, then later exhausted the flow without a final answer. Appending the missing issue-type qualifier only for the traced `github:search_issues` shape is a narrow MCP-layer recovery that removes one concrete source of wasted iterations and improves first-pass success for issue lookups.
- QA feedback: None (new iteration)
- Before evidence: Langfuse trace `session_1773077194122_1fq71nkbc` in session `conv_1773077194696_a5e9ax0da` records warning observation `25a2da3b-4a0c-450f-a64b-68751b44a745`, where `github:search_issues` was invoked with `q: "repo:aj47/dotagents-mono mobile session agent dropdown can't select"` and failed with `Validation Error: Query must include 'is:issue' or 'is:pull-request'`. The persisted conversation file `~/Library/Application Support/app.dotagents/conversations/conv_1773077194696_a5e9ax0da.json` then shows later tool calls repeating the same search intent with `is:issue` appended, but no final assistant answer was ever persisted, so the traced run both wasted an iteration and still failed to complete.
- Change: Updated `apps/desktop/src/main/mcp-service.ts` so the desktop MCP executor appends `is:issue` when `github:search_issues` receives a non-empty query missing any explicit `is:issue`, `is:pull-request`, or `is:pr` qualifier. Added focused regressions in `apps/desktop/src/main/mcp-service.option-b.test.ts` proving the traced missing-filter query is rewritten before the MCP call while explicit pull-request filters remain untouched. This ledger handoff commit records immutable evidence through substantive fix commit `b8b95732bd77f4ba0627ee2726d1b641b450fa6c`.
- After evidence: `apps/desktop/src/main/mcp-service.option-b.test.ts` now simulates the traced missing-filter search query and verifies the MCP client receives `... is:issue` instead of the validation-failing original string. A companion regression proves `is:pull-request` searches are preserved exactly, keeping the new sanitization aligned with the traced failure rather than broadening it into generic query rewriting.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/mcp-service.option-b.test.ts` → passed (`12 tests`, exit 0). `pnpm --filter @dotagents/desktop run typecheck:node` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. This fix intentionally defaults only missing-type `github:search_issues` queries to `is:issue`; if future Langfuse traces show PR-intent searches that still omit their qualifier, that should be handled with separate trace-backed guidance rather than inferred here. As with prior iterations, the current ledger handoff commit will need a later provenance backfill if QA requires the final docs-only commit SHA itself to be covered.

### 2026-03-11 - Short-circuit no-history “what next?” / “continue” prompts
- Reviewed `langfuse-bug-fix.md` first, then read the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` before choosing work.
- Explicitly deferred the older unresolved QA findings, because they concern prior evidence-provenance follow-up rather than a fresh user-facing failure. This iteration instead fixes a concrete single-run failure from an uninspected Langfuse trace.
- Chosen issue: session `conv_1772999860748_wn4zhhi4k` / trace `session_1772999860411_w8cklaaln` shows a bare no-history prompt `What should I do next?` returning `trace.output = null` after the agent still attempted tool work (`respond_to_user`, `execute_command`, and a warning `execute_command`) instead of asking for the missing context.
- Langfuse also shows the same conversation needed a follow-up trace `session_1773000351964_wfkuqtose` eight minutes later just to get `Still standing by—what would you like me to do next?`, confirming the original run did not satisfy the user in a single pass.
- Reconstructed the fix in the desktop agent entry paths: classic agent mode (`apps/desktop/src/main/llm.ts`) and ACP mode (`apps/desktop/src/main/acp-main-agent.ts`) both lacked a small runtime guard for low-context bare `what next` / `continue` prompts when there is no prior conversation history.

#### Evidence
- Evidence ID: `low-context-next-step-guard`
- Scope: Prevent no-history desktop agent runs from wasting a tool-driven turn on bare `what next?` / `continue` prompts that should first request the missing task context.
- Commit range: `b8b95732bd77f4ba0627ee2726d1b641b450fa6c..3067d3cfb463fbb01dcbf09f863d8aaf93fdefc3`
- Rationale: The traced run failed a basic user-intent standard: with no prior history, the agent still started acting, produced no final output, and forced the user to send another message to recover the same intent. A narrow runtime guard is safer than relying on prompt wording alone because the trace already occurred despite low-context guidance being present in the system prompt.
- QA feedback: Deferred older outstanding QA feedback on prior evidence-provenance-only notes for this iteration; chose a new Langfuse-backed user-facing failure instead.
- Before evidence: Langfuse trace `session_1772999860411_w8cklaaln` in session `conv_1772999860748_wn4zhhi4k` records input `What should I do next?`, `metadata.hasHistory = false`, `trace.output = null`, and span activity for `Tool: respond_to_user`, `Tool: execute_command`, and a warning `Tool: execute_command`. The same session later required follow-up trace `session_1773000351964_wfkuqtose`, which only replied `Still standing by—what would you like me to do next?`, confirming the original run neither clarified cleanly nor completed the user's intent.
- Change: Added a shared low-context prompt guard in `apps/desktop/src/main/agent-low-context-guard.ts` for bare no-history `what next` / `continue` prompts. Wired it into both `apps/desktop/src/main/llm.ts` and `apps/desktop/src/main/acp-main-agent.ts` so those runs now emit a direct clarification response and stop before tool execution. Added focused regression coverage in `apps/desktop/src/main/agent-low-context-guard.test.ts` plus an ACP integration regression in `apps/desktop/src/main/acp-main-agent.test.ts` proving the guard completes the run without calling `sendPrompt`. This ledger handoff commit records immutable evidence through substantive fix commit `3067d3cfb463fbb01dcbf09f863d8aaf93fdefc3`.
- After evidence: The new low-context guard tests now prove that bare no-history `What should I do next?` and `continue` prompts return an explicit clarification response, while contextual prompts like `What should I do next after pnpm test fails?` still pass through untouched. The ACP regression additionally proves the desktop ACP path persists the clarification response and never sends the traced prompt into the agent backend.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/agent-low-context-guard.test.ts src/main/acp-main-agent.test.ts` → passed (`16 tests`, exit 0). `pnpm --filter @dotagents/desktop exec tsc --noEmit -p tsconfig.node.json` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. The guard intentionally stays narrow to bare no-history `what next` / `continue` prompts; if future traces show broader ambiguous-input failures, expand this only with additional trace-backed evidence. Because the final handoff commit is ledger-only, later QA may still ask for a provenance backfill if it requires the docs commit SHA itself to be covered explicitly.

### 2026-03-11 - QA follow-up for classic low-context guard coverage
- Reviewed `langfuse-bug-fix.md` first and picked up the outstanding QA feedback from `../aloops/.langfuse-bug-fix-loop.qa-feedback.txt` instead of choosing a new trace.
- Re-inspected Langfuse trace `session_1772999860411_w8cklaaln` via `langfuse-cli`, confirming the no-history prompt still hit classic-agent tool activity before the later clarification follow-up: the observation list still included `respond_to_user`, `execute_command`, and warning `execute_command` with status `Skill not found: default`.
- Chosen follow-up: move the classic `processTranscriptWithAgentMode()` low-context guard ahead of profile/skills/memory/system-prompt loading, add a direct regression for that path, and correct the earlier Evidence ranges whose start SHA excluded the first authored commit.

#### Evidence
- Evidence ID: `low-context-next-step-guard`
- Scope: Close the remaining QA gaps on the traced no-history `what next?` failure by short-circuiting the classic agent path before heavy context loading and by backfilling truthful immutable provenance for the affected review-stack commits.
- Commit range: `3067d3cfb463fbb01dcbf09f863d8aaf93fdefc3..34d3fefa4f614a363e773acf6dff3609eed056df`
- Rationale: QA correctly noted that the earlier low-context fix only proved ACP behavior even though the Langfuse evidence showed the classic agent path still trying tools, and that several prior Evidence ranges excluded their own first authored commit. Moving the classic guard earlier aligns runtime with the trace-backed requirement to ask for missing context before loading repo/profile context, while the range corrections remove misleading provenance claims in the ledger.
- QA feedback: Addressed reviewer findings that (1) the low-context fix lacked a direct classic-agent regression for `processTranscriptWithAgentMode()` / `llm.ts`, and (2) earlier Evidence ranges for `github-list-issues-empty-since`, `inline-quoted-builtin-tool-directives`, `github-search-issues-missing-type-filter`, and `low-context-next-step-guard` used start SHAs that excluded the first authored commit.
- Before evidence: Re-inspection via `npx -y langfuse-cli api observations list --trace-id session_1772999860411_w8cklaaln --limit 20 --json` still showed the bare no-history prompt attempting classic-agent tool work (`respond_to_user`, `execute_command`, plus a warning `execute_command` with status `Skill not found: default`) before the user had supplied task context. In the ledger before this follow-up, the earlier low-context block still used commit range `851132bd9d0651cca0861a3856ed0e144994b196..3067d3cfb463fbb01dcbf09f863d8aaf93fdefc3`, which excluded substantive start commit `851132bd9d0651cca0861a3856ed0e144994b196`; the three earlier GitHub/tool-recovery Evidence blocks above had the same immutable-provenance problem.
- Change: Updated `apps/desktop/src/main/llm.ts` so `processTranscriptWithAgentMode()` persists the initial user message and then checks the shared low-context guard before loading agent profile data, skills, memories, or constructing the system prompt. Added `apps/desktop/src/main/llm.low-context-guard.test.ts` to prove a bare no-history `What should I do next?` prompt returns the clarification response, never loads profile/skills/memories or calls the LLM, and still persists conversation state. Also corrected the earlier Evidence ranges to start at their true parent commits (`f122b1b80ac0f66768dab76288ff2e5e9b8a4da9`, `fb8147fcc4a620118b5e0ed327970a32d27fca18`, `e178fbec8e94e6ed04b0dd3e1b22985837e8aa9e`, and `b8b95732bd77f4ba0627ee2726d1b641b450fa6c`) so those authored commits are now actually covered.
- After evidence: Targeted desktop regressions now cover both traced low-context entry points: ACP (`apps/desktop/src/main/acp-main-agent.test.ts`) and classic agent mode (`apps/desktop/src/main/llm.low-context-guard.test.ts`). The classic regression specifically verifies the guard short-circuits before profile/skills/memory/system-prompt work and before any LLM/tool execution, matching the trace-backed expectation that the app should ask for context first. The corrected ledger ranges now also truthfully include the earlier authored start commits QA identified.
- Verification commands/run results: `pnpm --filter @dotagents/desktop exec vitest run src/main/llm.low-context-guard.test.ts src/main/agent-low-context-guard.test.ts src/main/acp-main-agent.test.ts` → passed (`17 tests`, exit 0). `pnpm --filter @dotagents/desktop run typecheck:node` → passed (exit 0). Both commands emitted only the pre-existing unsupported Node engine warning for local Node `v25.2.1`.
- Blockers/remaining uncertainty: Runtime profile is `none`, so I did not launch Electron for a live repro. This pass keeps the guard narrow to bare no-history prompts and only moves it earlier in the classic desktop path. As usual for a ledger handoff, the current docs-only commit that records this Evidence block will itself need a later immutable-provenance backfill if QA requires the final handoff SHA to be covered explicitly.

