// Re-export from @dotagents/core — single source of truth
export {
  createLanguageModel,
  getCurrentProviderId,
  getCurrentModelName,
  getTranscriptProviderId,
} from '@dotagents/core'
export type { ProviderType } from '@dotagents/core'
