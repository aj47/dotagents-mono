diff --git a/android/build.gradle b/android/build.gradle
index 680ac0be9a0ce1a21c4d5ed74fc0da49398b0e8e..5fa49428273a49eb25087e62b0165de0fd069bec 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -184,7 +184,9 @@ gradle.taskGraph.whenReady {
 def expoModulesCorePlugin = new File(project(":expo-modules-core").projectDir.absolutePath, "ExpoModulesCorePlugin.gradle")
 apply from: expoModulesCorePlugin
 applyKotlinExpoModulesCorePlugin()
-useExpoPublishing()
+// The host Expo app configures its own publishing hooks. This module's
+// publishing block below is sufficient for SDK releases and avoids duplicate
+// Maven publications when consumed by an Expo app.
 // If you want to use the managed Android SDK versions from expo-modules-core, set this to true.
 // The Android SDK versions will be bumped from time to time in SDK releases and may introduce breaking changes in your module code.
@@ -215,12 +217,8 @@ android {
     versionName project.version
     buildConfigField "String", "SDK_VERSION", "\"${project.version}\""
   }
-  publishing {
-    singleVariant("release") {
-      withSourcesJar()
-      withJavadocJar()
-    }
-  }
+  // Expo's useExpoPublishing() already registers the release variant.
+  // Registering it again breaks AGP 8.6+ with a duplicate singleVariant error.
   lintOptions {
     abortOnError false
   }
